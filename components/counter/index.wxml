<l-counter l-count-class="all-background input-size"
           l-disabled-class="all-background"
           l-symbol-class="all-background font-color"
           l-class="container" bind:linout="onOverStep"
           count="{{count}}" min="{{min}}" max="{{max}}">
</l-counter>