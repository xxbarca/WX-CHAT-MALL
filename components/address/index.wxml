<view class="container">
    <l-button wx:wx:if="{{!hasChosen}}" bind:lintap="onChooseAAddress"
              plain="{{true}}"
              width="360"
              height="88"
              shape="square"
              icon="address"
              icon-size="28"
              icon-color="#157658"
              l-icon-class="sleeve"
              l-class="btn">选择地址</l-button>
    <image class="bottom-img" src="../imgs/bottom@address.png"></image>
</view>
